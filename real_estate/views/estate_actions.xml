<odoo>
    <record id="action_estate_properties" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first property
            </p>
        </field>
    </record>
    <!-- Property Types Action (NEW) -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

        <!-- Property Tags Action (NEW) -->
    <record id="estate_property_tags_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tags</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- ACTIONS -->
    <!-- Action for All Properties (No default filter) -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>

<!-- Dashboard Action -->
        <record id="estate_property_dashboard_action" model="ir.actions.client">
            <field name="name">Real Estate Dashboard</field>
            <field name="tag">estate_property_dashboard</field>
        </record>

</odoo>

